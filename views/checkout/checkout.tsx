"use client";
import { cn } from "@/shared/lib";
import { memo } from "react";

type Props = {};
const Checkout = ({}: Props) => {
  return <div className={cn("p-20")}>Checkout</div>;
};
export default memo(Checkout);
